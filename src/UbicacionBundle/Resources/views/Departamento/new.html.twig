    {% extends 'ThemeBundle:Default:layout_base.html.twig' %}
{% set title = 'Alta Departamento' %}
{% block title %}
{{ title }}
{% endblock %}
    {% block content -%}

    <h4 class="page-header">Departamento</h4>
    <div class="row">
        <div class="col-xs-12 col-sm-12">            
            <div class="box-header">
                <div class="box-name ui-draggable-handle">
                    <i class="fa fa-search"></i>
                    <span>Alta de Departamento</span>
                </div>
            </div>
            <div class="box-content">
                <h4 class="page-header">{{ title }}</h4>
                <form class="form-horizontal" role="form" action="{{ path('departamento_create') }}" role="form" method="post">
                    <div class="box-body">
                        {{ form_widget(form) }}
                    </div>
                    <div class="form-group">
                        <div class="col-sm-9 col-sm-offset-3">
                            <button class="btn btn-primary" type="submit"><i class="fa fa-save"></i> Guardar</button>

                            <a class="btn btn-success" href="{{ path('departamento') }}">
                                Volver
                            </a>

                        </div>
                    </div>
                </form>


            </div>
        </div>
    </div>


            {% endblock %}
